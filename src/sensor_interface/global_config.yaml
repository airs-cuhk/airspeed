# Active adaptor — change this to switch to a different sensor setup.
# The named folder must exist under this interface directory and contain
# a launch/start.sh script.
adaptor: "camera-stream-adaptor"
